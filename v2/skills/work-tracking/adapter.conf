# Active work-tracking backend adapter. One line: the adapter directory name
# under adapters/. The dispatchers (tracker.py, review.py) forward to
# adapters/<name>/. Defaults to "github" if this file is absent.
github
